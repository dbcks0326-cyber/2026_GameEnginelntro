using UnityEngine;

public class mynwe : MonoBehaviour
{


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("내가 이거 만들엇어");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
